config {
    type: "table",
    schema: "analytics",
    tags: ["analytics"],
    name: "dim_products",
    description: "Product dimension (one row per product).",
    columns: {
        product_id: "Product natural key (primary key).",
        category: "Product category.",
        product_name: "Product display name.",
    },
}

SELECT
    product_id,
    category,
    product_name
FROM
    ${ref("products_cleaned")}
